"use client";

/**
 * SceneBloom — temporarily disabled।
 * @react-three/postprocessing 3.0.4 React 19-এর সাথে compatible না।
 * (ReactCurrentBatchConfig crash)
 * package update হলে আবার enable করা যাবে।
 */
export default function SceneBloom() {
  return null;
}
